/**
 * Shared code between client and server
 * Useful to share types between client and server
 * and/or small pure JS functions that can be used on both client and server
 */

// Note: Since this is now JavaScript, we don't have TypeScript interfaces
// but we can still export utility functions and constants that work in both client and server
