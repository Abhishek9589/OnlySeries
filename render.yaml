services:
  - type: web
    name: fusion-backend
    runtime: node
    plan: free
    region: oregon
    envVars:
      - key: NODE_VERSION
        value: 20
    buildCommand: npm install && npm run build
    startCommand: npm run start
    healthCheckPath: /health
    autoDeploy: true
